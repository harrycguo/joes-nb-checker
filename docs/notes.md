# Artifact Viewer — demo notes

This is a **throwaway** content repo for testing the product-artifact-viewer with
a GitHub App. Nothing here is real/confidential.

- `notes.md` — this file, to test **markdown rendering** (`?raw` for source).
- `welcome.html` — a static HTML artifact.
- `demo/shared-counter.html` — an **interactive** artifact using the shim
  (shared state via Redis).

| thing | value |
|-------|-------|
| repo  | joes-nb-checker |
| mode  | GitHub App (installation token) |
